// Prefetcher: consumes prefix.access.v1, runs the Markov next-block model,
// emits cache.warm.v1 (DESIGN.md §4.4). M3 work — stub until then.
package main

import "log/slog"

func main() {
	slog.Info("prefetcher is M3 work — see docs/MILESTONES.md")
}
